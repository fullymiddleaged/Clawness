{
  "name": "clawness-openclaw",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw adapter for Clawness — shells out to the bundled Python engine and translates hook payloads.",
  "type": "module",
  "main": "dist/src/index.js",
  "exports": {
    ".": "./dist/src/index.js"
  },
  "engines": {
    "node": ">=22.19"
  },
  "openclaw": {
    "extensions": ["dist/src/index.js"],
    "plugin": {
      "json": "openclaw.plugin.json"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/test/*.test.js\"",
    "plugin:check": "plugin-inspector inspect --no-openclaw",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "@openclaw/plugin-inspector": "^0.3.21",
    "@types/node": "^22.5.0",
    "typescript": "^5.6.0"
  }
}
